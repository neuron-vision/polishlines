{
  "name": "hosting",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite",
    "functions": "firebase emulators:start --only functions",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:rules": "firebase deploy --only database --project polish-lines",
    "deploy:hosting": "npm run build && firebase deploy --only hosting --project polish-lines",
    "deploy": "npm run build && firebase deploy --project polish-lines"
  },
  "dependencies": {
    "firebase": "^11.0.0",
    "onnxruntime-web": "^1.23.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-hot-toast": "^2.6.0",
    "react-router-dom": "^7.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^6.3.5"
  }
}
